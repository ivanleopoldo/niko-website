name: Secret Keys

on:
  push:
    branches: [ main, dev]

jobs:
  get-secrets:
    runs-on: windows-latest
    steps:
      - name: 'create .env'
        run: |
          echo "${{ secrets.ENV_FILE }}" > .env

      
